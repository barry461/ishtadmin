version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "9000:9000"
    volumes:
      - .:/var/www/html
    environment:
      - PHP_FPM_LISTEN=9000
    networks:
      - yaf-network

networks:
  yaf-network:
    driver: bridge
